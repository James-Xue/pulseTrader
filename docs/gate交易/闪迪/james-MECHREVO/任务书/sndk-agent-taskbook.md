# SNDK 闪迪合约子代理 — 任务书(家庭机 james-MECHREVO 版)

> 每次批次开始读本文件;完成后更新。任务状态:⬜ 待办 / 🔄 进行中 / ✅ 完成
> 本机接管公司机代理的网格(同前缀 t-sndk-grid-*);公司机断网,交易所侧挂单为唯一事实源

## 当前任务(网格 v2.5 扩展式,2026-08-20 凌晨接管)

1. **✅ 接管**:交易所现有 13 格空单 1600~1660(步进5,2张/格)+ 5 笔 TP 买单(1555~1575,对应 1565~1585 已成交格),持仓 0
2. **🔄 网格维护(每轮 ~25s)**:`python3 sndk_grid.py status` → 核对 → 行动:
   - **成交格挂 TP**:网格空单成交且无对应 TP → `python3 sndk_grid.py tp <成交价>`(reduce_only 买单 @成交价-10,2张)
   - **TP 兑现重挂**:TP 买单成交且现价低于该格 → `python3 sndk_grid.py place <格价>`
   - **缺格补挂**:12+ 格中某格无挂单且 mid 未涨过该格 → 补挂
3. **🔄 扩展式跟随(今晚实测 v2.5,0 撤单原则)**:
   - 价格上行:顶格落后于现价 >5 点 → 在顶部按步进 5 加档,顶格 ≈ round(last+5, 5)
   - 价格远离下方(mid < 最低格-15)→ 下方开新档接反弹
   - 格数动态,不撤单(除非协议显式要求)
4. **🔄 保护线(夜间自主模式,见下节)**:A:15m 收盘 > 顶格+20;B:网格浮亏 ≤ -30 USD
5. **⬜ 红窗执行(今晚重点)**:见下方"红窗协议"

## 🌙 夜间自主模式(2026-08-20 00:5x ~ 08:00 北京,用户睡眠,勿打扰)

- **保护线 B(网格浮亏 ≤ -30 USD)触发 → 自动执行**:全平网格持仓(reduce_only 单)+ 撤全部 t-sndk-grid-* 挂单 → **本日休眠**(不再开任何新单,TP 残单可撤)→ 记录+报警(存档用,不期待回复)。理由:用户无法响应,"建议全平"按用户预设阈值自主落实。
- **保护线 A(15m 收 > 顶格+20)触发 → 不自动平**:停止一切加档,TP 照常,记录+报警。若随后浮亏达 B 阈值 → 按 B 自动执行。
- **红窗内急涨穿网格**:不等待用户,按 B 阈值自动执行。
- **日亏停手**:网格自身今日已实现 ≤ -10 USD → 停开新单(照旧,自主执行)。
- **其余策略调整**:自主决策,思考板记录理由(授权不变)。
- **与主会话通信**:报警/简报照发(存档用),不期待任何回复;禁止以"等待用户"为理由放弃保护动作。

## ⛔ 红窗协议 — FOMC 会议纪要(2026-08-20 02:00 北京,red 事件)

- **窗口**:01:30 ~ 02:45 北京时间(2026-08-19T17:30Z ~ 18:45Z UTC)
- **窗内禁止**:任何新空单(加档/重挂/补挂全部暂停);重锚
- **窗内允许**:TP 买单照常挂(减仓方向);已有挂单照看;保护线检查加密
- **窗内异常**:价格急涨穿网格 → 保护线 A/B 立即报警,等用户
- **窗前后**:01:30 前最后 1 轮做快照(价格/持仓/浮亏);02:45 后首轮做事件复盘简报发主会话

## 硬红线(不可动,即使"自主更新策略"授权也无效)

1. 只做空;单格 2 张;日亏停手:网格自身今日已实现 ≤ **-10 USD**(UTC 日计)→ 停开新单
2. **不碰用户 App 挂单**(cancel 脚本已内置前缀校验,仅可撤 t-sndk-grid-*)
3. 不自动平用户仓位;不碰其他品种(XAUUSD/BTC/UNITREE/ETH 等)
4. 网络/API 失败 → 跳过本轮,绝不基于过期数据下单;连续 3 轮失败 → 报警

## 自主更新策略授权(用户 2026-08-20 00:5x 口头授权)

- 可调整:加档节奏/格数上限/保护线阈值/红窗规则细节 —— **必须**写入思考板记录理由
- 不可动:硬红线(上节);TP 幅度 -10 如需改动必须先问用户
- 每次策略调整同时更新本任务书"策略变更"节

## 运行方式

- 工具:`0_note/gate交易/闪迪/james-MECHREVO/工具/sndk_grid.py`(REST 直连,直连失败自动回退代理)
- 状态:`…/状态/sndk-agent-state.json`;思考板:`…/思考板/sndk-agent-thinking.md`(最新在前)
- 每批 10 轮,批末 SendMessage 主会话简报;保护线/异常立即报
- 父会话看门狗 cron 每 10 分钟查状态新鲜度,超 6 分钟自动接力
- 引擎视角仅作参考:本机引擎是 CFD 版(5daeaae),SNDK 一切以交易所 REST 为准

## 策略变更记录

- 2026-08-20 00:5x:接管公司机 v2 网格,采用今晚实测的扩展式策略(v2.5);新增 FOMC 红窗协议(02:00 北京)
- 2026-08-20 05:2x:公司机复联成为第二写者(事件#1/#2 幻影 TP 削单平仓、#3 正确 TP 重复挂单)。本机代理转入**保护者模式**:停止一切新挂单(place/tp),仅监控+保护线 B 自动全平+日亏红线;缺 TP 不补(让公司机对账修复);连续 3 轮无外部活动且存在缺口才补挂一笔后回保护者模式;重复单不撤不重挂;公司机 v2 重锚撤单行为不反击,记录+报警。保护线 B 优先级最高。
- 2026-08-20 06:5x:公司机对账已连续正确(事件#6 重复 @1595、#7 补挂 tp-1610@1600),对账权让出。**恢复维护门槛提高为:公司机静默 ≥30 分钟**(状态文件记录 last_external_activity)+ 有缺口 + 无重复单 + 现价距目标 TP 价 ≥5 点,四条件同时满足才补挂一笔;否则保持严格保护者。
