# ETH 以太坊合约子代理 — 任务书 v1.1(接管版)

> 每次会话开始读本文件;完成后更新。任务状态:⬜ 待办 / 🔄 进行中 / ✅ 完成
> 规则细节以 `策略/eth-subagent-strategy.md` 为准;规则歧义先问用户

## ⚠️ 接管公告(2026-08-20 00:50 北京)

- **ETH 网格已实盘运行**(joey 机断网前部署,本机接管管理)。**不要新建网格,接管现有网格!**
- 交易所侧现状(00:44 直连 REST 实测):
  - 持仓:**ETH_USDT -404 张 @2046.14(浮亏 -170.5)**——含用户手动仓位,网格代理份额只占其中已成交格的部分
  - open 挂单 14 笔:`t-eth-grid-tp-*` reduce-only 买单 13 笔(2010~2070,每笔 2 张)、`t-eth-grid-2110/2115` 空单 2 笔(每笔 2 张)、**用户 App 挂单 `app` -100 @2128(不碰!)**
  - futures 账户:total 1192.8 / available 801.3 USDT;CFD 50 USD;spot 0.06 USDT
  - 行情:mark ~2097,现价在网格已成交段上方、剩余空单格(2110/2115)下方
- **用户今晚授权(00:42)**:ETH 子代理**通宵盯盘 + 正式交易 + 随时更新自己的策略和动态网格范围**。
- **夜间自主模式(00:53 追加,至 08:00 北京)**:用户已入睡,**禁止向用户提问/等待用户决策,一切动作自主决定并落盘**(思考板/复盘/状态文件),用户 08:00 后阅读文件即可。保护线 A/B 触发后按 §4 夜间自主版执行(不报警等人,直接行动)。
- **重大事件**:用户提示 **今晚 02:00 左右(北京)美国重大事件公布**(FOMC 会议纪要,日历红字;日历另记 00:00Z 有出入,以用户 02:00 北京 = 18:00Z 为准)。**事件窗口 01:30–02:30 北京(17:30–18:30Z)**:不重锚、不补挂新格、不撤 TP;只做 TP/保护线管理与加倍核对;02:30 后恢复常规。
- **工具**:交易所侧一切操作走 `工具/eth_ledger.py`(orders/positions/account/klines/book/place/tp/cancel,直连签名 REST);MCP 仅只读(status/account/positions/get_orders)。**引擎今晚不改配置不重启**(黄金代理在用)。
- 红线(不可自主改动):**只做空 / 每格 2 张 / 单格无止损 / 日亏 -10 USD 停手 / 保护线 B 网格浮亏 ≤ -30 USD 报警由用户决定 / 不碰 `app` 前缀与用户仓位份额 / 严禁 place_trigger_order / 严禁 reduce_only 缺失**。

## ⚠️ 暂停挂网格(2026-08-20 04:56 父会话指令,至用户指示恢复)

- 04:51 价格暴冲(2124→2217,+117 点):用户市价买 486 张 @2217.52 **清仓全部**(含网格 2110/2115 成交与 app 三单成交),挂单全清。现 0 持仓 0 挂单。
- **代理仅监控,禁止挂新网格/重锚/下单**(state.hold=true);恢复须用户指示或父会话转达。监控职责照旧:每轮核对账户/持仓/挂单,保护用户不被异常状态影响,异常落盘。
- 若用户 App 重新挂单/开仓:只观察,不干预,不跟单。

## 当前任务

1. **🔄 接管核对**(每轮):`eth_ledger.py orders` 与 positions 对照,维护网格完整性:
   - 13 笔 TP 买单(2010~2070)在列且 reduce_only=true;2110/2115 空单在列
   - 用户 `app` 挂单只看不动;`t-eth-grid-*` 之外的任何单不碰
2. **🔄 循环重挂**:TP 买单成交(挂单消失且价格曾 ≤ TP 价)→ 若现价低于原格价,同格重挂空单 + 新 TP(经典网格)
3. **🔄 补挂缺失格**:网格已成交格 TP 兑现后重挂;2110/2115 两格若成交(价格涨穿)→ 立即挂 TP(成交价 - 2×step);**是否向下扩展新格由代理按策略 §3.2/§5 自主决定**(自主更新网格范围授权)
4. **🔄 重锚检查**:按策略 §3.1(mid < 下沿 - 3×step 下移跟随;mid > 上沿 + step 不自动重锚转保护线 A;冷却 30 分钟)
5. **🔄 保护线(夜间自主版)**:A:15m 收盘 > 网格顶 + 4×step(现网格顶 2115 → 15m 收盘 > 2135)→ **自主行动**:reduce_only 市价买入平掉网格自己份额(按状态文件记录的网格已成交格数 × 2 张计算,严禁超过该份额)→ 落盘记录 → 按 §3.1 在新区间重新锚定挂格;B:网格浮亏 ≤ -30 USD → 同样自主平掉网格份额 + 落盘 + 重锚。**两条线都不再等人**。
6. **🔄 事件窗口**:01:30–02:30 北京 = 特殊规则(见接管公告);事件前每轮在思考板记价格/波动状态;事件后按场景自主处置:价格暴跌→TP 兑现按循环重挂;价格暴涨穿网格→保护线 A 自主版。
7. **🔄 策略/网格范围自更新**(授权):step/锚/TP 距离/格数/冷却可在红线内自主调整,调整必须:① 写进策略文档变更记录 ② 思考板记录理由 ③ 状态文件同步
8. **🔄 夜间生存法则**:每轮把上下文完整落盘(状态文件含网格已成交格数、持仓份额、锚、冷却截止);不依赖上一轮记忆——每轮从交易所侧+状态文件重新推导;API 报错跳过本轮;不确定就什么都不做
9. **⬜ 多代理并行验证**:与黄金/闪迪/宇树代理互不干扰(引擎共享只读,ETH 下单直连)

## 参数基线(v2 拍板 2026-08-21;红线不可改,其余代理可自主调整并落盘)

| 参数 | 值 |
|---|---|
| 方向 | 只做空(红线) |
| step | **ATR 自适应 clamp(round(0.5×ATR15m),3,8)**(v2 正式落地;每次更新落盘 state.grid.step,tp_distance=2×step 同步) |
| TP | 成交价 - 2×step |
| 每格张数 | 2(红线) |
| **趋势闸门(v2)** | **15m EMA 空头排列才挂新格/重锚;多头排列只管理存量(TP/保护线),state 记 trend_gate=bullish** |
| **暴拉冻结(v2)** | **state.grid.spike 存在(1m 涨幅>max(1%,3×ATR15m),watchdog 检测落盘)→ 冻结新格 30 分钟** |
| 保护线 A(v2) | **15m 收盘 或 1m 收盘 > 顶+2×step**(不再等人,自主平网格份额+落盘+按 §3.1 重锚) |
| 保护线 B | 网格浮亏 ≤ -30 USD → 同 A |
| 日亏停手 | 已实现亏损 ≤ -10 USD(红线),**北京日 08:00 重置**(v2 拍板口径) |
| 重锚冷却 | 30 分钟 |
| 信号源 | get_signals 的 eth_scalper_ETH_USDT(EMA bearish cross + 暴拉过滤)可作趋势闸门补充参考 |

## 已完成

- **2026-08-20 07:43:任务结束(用户指令关闭子代理)**——relay 循环 / watchdog / 每小时巡检 / 日志监控全部停止;state.mode=stopped,hold 与日亏停手记录保留;恢复须用户明确指示。关闭时快照:ETH -210 与 `app` 挂单均为用户 App 活跃交易,代理未干预。全程 51 个 LLM 回合 + watchdog 兜底,网格实亏 -38.6 定格。晨报:`复盘/eth-morning-report-20260820.md`。
- 2026-08-20 04:52:**round22 重大事件**——价格 04:50-04:51 暴涨 2128→2222 穿网格,用户 App 自动单(ao-)04:51:06 单笔买 486@2217.52 全平(网格 24 + 用户 462),网格归零、0 挂单、账户 total 1184.59→477.85;网格份额实亏 ≈ -39.2 → **日亏停手(-38.6 ≤ -10 红线),当天不再开新仓/重挂**。与 watchdog 判定一致(hold/flattened 已置位)。复盘 `复盘/eth-review-20260820-045200.md`。
- 2026-08-20 00:50:**接管版任务书 v1.1**——交易所侧现状实测 + 用户授权(通宵盯盘/正式交易/自主更新)+ FOMC 事件窗口
- 2026-08-20 00:44:**账户/持仓/挂单实测**(futures total 1192.8/avail 801.3;-404 @2046 浮亏 -170.5;14 笔 open)
- 2026-08-20 00:40:策略文档 v1 创建(镜像 SNDK v2);`工具/eth_ledger.py` 工具箱
- 2026-08-20 00:22:目录骨架创建(SNDK 会话)

## 约定

- 网格单:`eth-grid-<price>`;TP 买单:`eth-grid-tp-<成交价>`;代理其他单:`eth-agent-` 前缀
- 撤单:只允许撤自己前缀 `eth-grid-*` / `eth-grid-tp-*`;用户 `app` 前缀绝对不碰
- 状态/复盘:`状态/eth-agent-state.json` / `复盘/eth-review-*.md`;每笔平仓写复盘
- 保守优先:**不确定就什么都不做**;API 报错跳过本轮,绝不在错误状态重试下单
- ⚠️ **父会话机械 watchdog(`工具/eth_watch.py`,后台常驻,150s/轮)是底线保护进程:禁止 kill/stop/修改/编辑它**(它负责 TP 完整性重挂与保护线自动处置,与代理职责互补)。看到它报错只记录不干预,由父会话修复。同样的,relay 循环(`工具/eth_relay.sh` 的 while 循环)也禁止终止。
